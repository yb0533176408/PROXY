name: VMess+WS proxy via Cloudflare Tunnel

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: proxy-server
  cancel-in-progress: true

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Tune kernel for high-RTT throughput (BBR + big buffers)
        run: |
          sudo sysctl -w net.core.default_qdisc=fq
          sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
          sudo sysctl -w net.core.rmem_max=268435456
          sudo sysctl -w net.core.wmem_max=268435456
          sudo sysctl -w net.ipv4.tcp_rmem="4096 87380 268435456"
          sudo sysctl -w net.ipv4.tcp_wmem="4096 65536 268435456"
          sudo sysctl -w net.ipv4.tcp_fastopen=3
          sudo sysctl -w net.ipv4.tcp_mtu_probing=1
          echo "cc=$(sysctl -n net.ipv4.tcp_congestion_control) qdisc=$(sysctl -n net.core.default_qdisc)"

      - name: Download Xray-core
        run: |
          curl -L -o xray.zip https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip
          unzip -o xray.zip xray 2>/dev/null || unzip -o xray.zip
          chmod +x xray
          ./xray version | head -2

      - name: Run Xray (VMess + WS on :8080)
        run: |
          nohup ./xray run -c xray_config.json > xray.log 2>&1 &
          sleep 4
          ss -ltnp 2>/dev/null | grep ':8080' && echo "xray OK on 8080" || { echo "xray NOT up:"; cat xray.log; exit 1; }

      - name: Install cloudflared
        run: |
          curl -L -o cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb
          cloudflared --version

      - name: Build tunnel credentials from token
        env:
          CF_TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
        run: |
          python3 - <<'PY'
          import os, base64, json
          t = os.environ['CF_TUNNEL_TOKEN'].strip()
          d = json.loads(base64.b64decode(t + '=='*(-len(t) % 4)))
          json.dump({"AccountTag": d["a"], "TunnelID": d["t"],
                     "TunnelSecret": d["s"], "Endpoint": ""},
                    open("/home/runner/creds.json", "w"))
          print("token is for tunnel:", d["t"])
          PY

      - name: Run Cloudflare Tunnel
        run: |
          nohup cloudflared tunnel --config cloudflared.yml run > cf.log 2>&1 &
          for i in $(seq 1 20); do
            grep -qiE "Registered tunnel connection" cf.log && break
            sleep 2
          done
          echo "--- cloudflared log ---"; tail -20 cf.log
          grep -qiE "Registered tunnel connection" cf.log || echo "WARN: tunnel not registered yet"

      - name: Self-check
        continue-on-error: true
        run: |
          curl -s -o /dev/null -w "local  8080/xh        -> %{http_code}\n" http://127.0.0.1:8080/xh
          for i in 1 2 3; do
            curl -s -o /dev/null -w "edge  1.tsoolgee.uk/xh -> %{http_code}\n" https://1.tsoolgee.uk/xh || true
            sleep 3
          done

      - name: Keep alive (~5h40m, under 6h cap) + restart cloudflared if it dies
        run: |
          for i in $(seq 1 340); do
            if ! pgrep -f "cloudflared tunnel" > /dev/null; then
              echo "$(date -u +%H:%M:%S) cloudflared died - restarting"
              nohup cloudflared tunnel --config cloudflared.yml run >> cf.log 2>&1 &
            fi
            if ! pgrep -f "xray run" > /dev/null; then
              echo "$(date -u +%H:%M:%S) xray died - restarting"
              nohup ./xray run -c xray_config.json >> xray.log 2>&1 &
            fi
            sleep 60
          done
